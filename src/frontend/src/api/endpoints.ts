const URLS = {
  auth: {
    login: "/auth/login",
    signup: "/auth/signup",
    authenticate: "/auth/authenticate",
    profile: "/auth/profile",
    refresh: "/auth/refresh",
  }
};

export default URLS;